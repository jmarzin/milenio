# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /about                      controllers.Application.about
GET     /services                   controllers.Application.services
GET     /contact/:focus_message     controllers.Contact.edit(focus_message: Boolean )
GET     /portefeuille               controllers.Application.portefeuille
GET     /realisation/:real          controllers.Application.realisation(real)
POST    /contact/message            controllers.Contact.message

# Map static resources from the /public folder to the /assets URL path

GET     /assets/*file               controllers.Assets.at(path="/public", file)
